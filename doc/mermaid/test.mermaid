graph TD
    A[客户端] -->|1. 发送请求 GET key| B(集群任意节点);
    B -->|2. 计算哈希槽| C(哈希槽);
    C -->|3. 检查槽位是否归属自己| D{槽位是否归属B?};
    D -->|4. 是| E[在本地处理请求，返回结果];
D -->|4. 否| F[返回MOVED 槽位，正确节点地址];
F -->|5. 客户端更新本地映射表| G[客户端];
G -->|6. 向正确节点重新发送请求| H(正确节点);
H -->|7. 在本地处理请求，返回结果| I[返回结果];